# Write a program that takes a string as input and returns the number of characters in the string.
def count_characters(str):
   return len(str)
user_input = input("enter an integer")
characters_count = count_characters(user_input)
print("the num of character are",characters_count)